#ifndef consonantize_Included
#define consonantize_Included

#include <string>

/**
 * Given a string, converts that string into an Only Consonants puzzle by deleting all
 * non-consonants and making all characters upper-case.
 *
 * @param phrase The phrase to transform.
 * @return That string, rendered as an Only Consonants puzzle.
 */
std::string consonantize(std::string phrase);

#endif
